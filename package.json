{
  "name": "executive-profile",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "cd my-react-app && npm run dev",
    "dev": "cd my-react-app && npm run dev",
    "build": "cd my-react-app && npm run build",
    "test": "cd my-react-app && npm run test"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
